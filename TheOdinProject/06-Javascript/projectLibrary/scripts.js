let myLibrary = [

];

function Book() {
  // the constructor...
}

function addBookToLibrary() {
  // do stuff here
}



/*
function Book(title, author, pages, read) {
  this.title = title;
  this.author = author;
  this.pages = pages;
  this.read = read;
}

Book.prototype.info = function() {
  return `${this.title}, ${this.author}, ${this.pages}, ${this.read}`;
}

let firstBook = new Book('Night Angel Trilogy', 'Brent Weeks', 'Lots of Pages', 'Ready it immensely');

console.log(firstBook.info());
*/